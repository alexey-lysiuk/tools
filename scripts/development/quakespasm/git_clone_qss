#!/bin/sh

set -o errexit

SRC_REPO_DIR=/Volumes/Storage/Work/mirrors/quakespasm-spiked

cd "${SRC_REPO_DIR}"
git fetch --all

cd /Volumes/ramdisk
git clone -s "${SRC_REPO_DIR}" quakespasm-spiked$1

cd quakespasm-spiked$1
git remote remove origin
git remote add origin https://github.com/alexey-lysiuk/quakespasm-spiked.git
git remote add upstream https://github.com/Shpoike/Quakespasm.git
git fetch --all
git pull upstream master
git branch -u origin/master
